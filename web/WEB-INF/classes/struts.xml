<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
    <!-- struts2的开发模式 -->
    <constant name="struts.devMode" value="false"/>
    <!-- struts配置文件改动后，是否重新加载 -->
    <constant name="struts.configuration.xml.reload" value="true"/>
    <!-- 设置浏览器是否缓存静态内容 -->
    <constant name="struts.serve.static/browserCache" value="false"/>
    <!-- 请求参数的编码方式 -->
    <constant name="struts.i18n.encoding" value="utf-8"/>
    <!-- 每次HTTP请求系统都重新加载资源文件，有助于开发 -->
    <constant name="struts.i18n.reload" value="true"/>
    <!-- 文件上传最大值 -->
    <constant name="struts.multipart.maxSize" value="104857600"/>
    <!-- 让struts2支持动态方法调用 -->
    <constant name="struts.enable.DynamicMethodInvocation" value="true"/>
    <!-- Action名称中是否还是用斜线 -->
    <constant name="struts.enable.SlashesInActionNames" value="false"/>
    <!-- 允许标签中使用表达式语法 -->
    <constant name="struts.tag.altSyntax" value="true"/>
    <!-- 对于WebLogic,Orion,OC4J此属性应该设置成true -->
    <constant name="struts.dispatcher.parametersWorkaround" value="false"/>

    <!--<package name="basePackage" extends="struts-default">
         <action name="/" class="daniel.action.UserAction"> <result>index.jsp</result>
            </action>
    </package>-->

    <!-- 把struts的请求委托给spring管理,<include file="java/org/goodsManagement/controller/struts-Inrepository.xml"></include>
         作用:创建Action实例的过程由spring处理,其他的还是有struts2自己处理 -->
    <constant name="struts.objectFactory" value="spring" />
    <package name="inrepository" extends="struts-default" namespace="/">
        <action name="inrepository" method="add" class="org.goodsManagement.controller.InrepositoryAction">
            <result>/addsusess.jsp</result>
        </action>
    </package>
</struts>